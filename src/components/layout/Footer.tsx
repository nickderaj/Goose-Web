export default function Footer() {
    return (
        <footer className="mt-auto w-full border-t border-t-foreground/10 p-8 flex flex-col gap-2 justify-center text-center text-xs">
            <p>Eat, Share, Honk!</p>
        </footer>
    );
}
